# ProductPhotographyPiyush

Expo (TypeScript) app containing the Photography screen.

## Setup (run locally)

1. Install dependencies:

2. Start Expo:

3. Open on device/emulator:
- Press `a` in the terminal to open on Android emulator (or use Expo DevTools -> Run on Android device/emulator).
- Or scan the QR code with Expo Go on your phone.

## Files of interest
- `screens/PhotographyScreen.tsx`
- `screens/LoginScreen.tsx`
- `assets/` : image assets used in the screen
